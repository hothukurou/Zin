import { loadImages, mapHeight, mapWidth } from "./general";
import { Point2d } from "./types";

/*
export class PieceObject extends Phaser.GameObjects.Container {
    constructor(scene: Phaser.Scene, onClick: (pieceObject: PieceObject) => void) {
        super(scene, 0, 0);


        image.on("pointerup", () => {
            onClick(this);
        });
        this.add(image);
        scene.add.existing(this);
    }
}
*/